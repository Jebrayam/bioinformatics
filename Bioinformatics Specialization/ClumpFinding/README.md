# Clump Finding Function
**ClumpFinding(genome, k-mer, L-window, t-times)**: Finds patterns forming clumps in a string.

**Note:** This algorithm runs slow with very long sequences , but it does the work done.
